# Fichiers statiques

Dépose ici les assets servis à la racine du site, par exemple :

- **Logo** : `logo.png` ou `logo.svg`  
  → utilisé dans le code par `/logo.png` ou `/logo.svg`

Ces fichiers ne sont pas modifiés au build et restent accessibles à la racine.
